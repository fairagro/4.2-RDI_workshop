---
const { address, city, country, mapSrc } = Astro.props;
---

<section class="bg-light-gray dark:bg-dark-gray p-5 rounded-lg shadow-md mb-5 border border-gray-200 dark:border-gray-700">
  <h2 class="text-teal dark:text-lime text-2xl mb-3">Location</h2>
  <div class="mb-3">
    <p class="text-lg text-black dark:text-white mb-1">{address}</p>
    <p class="text-lg text-black dark:text-white mb-1">{city}</p>
    <p class="text-lg text-black dark:text-white mb-1">{country}</p>
  </div>
  <div class="w-full h-64 rounded-lg overflow-hidden">
    <iframe
      src={mapSrc}
      width="100%"
      height="100%"
      style="border:0;"
      allowfullscreen=""
      loading="lazy"
      referrerpolicy="no-referrer-when-downgrade"
      title="Google Map Location - Leibniz-Gemeinschaft"
    ></iframe>
  </div>
</section>
